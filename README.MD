# Проект "Интернет-магазин"

## Описание:

Проект "Интернет-магазин" - это приложение на Python, 
которое выполняет функции электронной торговли.


## Установка:

+ Установите на свой компьютер GitHub.
+ Клонируйте репозиторий с GitHub с помощью веб-URL.
```
 git clone git@github.com:den-solodovnikov/HomeWork_Mod_4.git
```


## Использование:

В текущем виде, программа запускается в модуле main.py:
Для проверки работы функций в модуле main.py


## Примеры использования функций:

+ Модуль product содержит функцию инициализации класса Product

**class Smartphone(Product):**
Класс 'Смартфон' является наследником класса 'Product'

**class LawnGrass(Product):**
Класс 'Трава газонная' является наследником класса 'Product'.

**def new_product(cls, new_product_dict: dict) -> Product:**
Функция создания нового продукта класса Product.

**def price(self, new_price: float) -> None:**
Функция-сеттер для установки новой цены товара с проверкой (отрицательная цена, цена ниже текущей).

**def __add__(self, other):**
Функция сложения товаров только из одинаковых классов продуктов.
При попытке сложения товаров из разных классов (например траву газонную и смартфон) 
выбрасывается ошибка TypeError.

+ Модуль category содержит функцию инициализации класса Category

**def add_product(self, new_product) -> str | None:**
Функция добавляет новый продукт в класс Category.
Если такой товар уже присутствует, добавляет количество товаров.
Ничего кроме смартфонов, травы газонной или других продуктов
в список добавить нельзя.

**def products(self) -> str:**
Функция-геттер выводит строки, состоящие из списка продуктов с ценой и остатками.

+ Модуль category_iterator содержит функцию инициализации класса CategoryIterator
**class CategoryIterator:**
Класс принимает на вход объект класса Category и производит итерацию по товарам,
которые хранятся в данной категории. 
Выполнение следующего шага итерации возвращает очередной товар категории.

+ Модуль utils
содержит функции подгрузки данных из JSON-файла:

**read_json(path) -> dict:**
Открывает файл JSON и считывает данные в словарь

**create_objects_from_json(data: dict) -> list:**
Создает объекты классов Product и Category из данных, полученных из внешнего файла.

## Разработка:

+ Проект находится в активной разработке. План работы в дальнейшем:

+ Добавление новых модулей для расширения функционала

+ Улучшение обработки различных форматов данных

+ Добавление тестов


## Автор проекта:

 Denis Solodovnikov